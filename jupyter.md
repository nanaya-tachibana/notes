# config

``` python
c.NotebookApp.iopub_data_rate_limit = 1e10  # 支援ploty
c.NotebookApp.ip = '*' # 服务器IP, *表示允许IP访问, ''表示只允许本地访问
c.NotebookApp.port = 8888 # 端口号, 默认是8080
c.NotebookApp.open_browser = False # 是否自动打开浏览器
c.NotebookApp.notebook_dir = '/pass'  # 路径
```
